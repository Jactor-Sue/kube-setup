apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: busybox
  namespace: default
spec:
  replicas: 2
  containers:
  - image: busybox
    command:
      - sleep
      - "3600"
    imagePullPolicy: IfNotPresent
    name: busybox
  restartPolicy: Always
