function find()
{
  for name in `ls $1`
  do
    if [ -d $1/$name ]
    then
      find $1/$name
    else
      res=$(cat $1/$name | grep image)
      if [ "$res" ]
      then
        echo $res
      fi
    fi
  done
}

find .
